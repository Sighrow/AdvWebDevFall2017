extends layout

block content
  h1= title
  form(method="post" action="")
    label Grid size:
    br
    select(name="size" id="size")
        +loadDDL
    input(type="submit" value="Submit")
  p= msg
  table
    +newRow(size)